<h1>Listing faq_categories</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @faq_categories.each do |faq_category| %>
      <tr>
        <td><%= faq_category.title %></td>
        <td><%= link_to 'Edit', edit_faq_category_path(faq_category) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Faq category', new_faq_category_path %>
